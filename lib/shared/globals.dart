import 'package:flutter/material.dart';

const addKey = GlobalObjectKey<FormState>('add_key');
const savedDBValues = 'saved_values';
const dbId = 'id';
const dbName = 'name';
const dbUrl = 'url';
const regExpUrl = r'https?://([\w-]+\.)+[\w-]+(/[\w-./?%&=#]*)?';
const maxUrlLength = 2000;
